apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "ocp-dog.fullname" . }}-frontend
  labels:
    {{- include "ocp-dog.labels" . | nindent 4 }}
    tier: frontend
spec:
  host: {{ .Values.route.frontend.host }}
  to:
    kind: Service
    name: {{ include "ocp-dog.fullname" . }}-frontend
    weight: 100
  port:
    targetPort: http
  {{- if .Values.route.frontend.tlsTermination }}
  tls:
    termination: {{ .Values.route.frontend.tlsTermination }}
  {{- end }}
  wildcardPolicy: None
